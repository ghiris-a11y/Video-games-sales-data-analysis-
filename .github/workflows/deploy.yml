---

### ✅ 4. **GitHub Actions for CI/CD** (`.github/workflows/deploy.yml`)
```yaml
name: Streamlit Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install -r requirements.txt

      - name: Run tests
        run: |
          echo "No tests yet"

      - name: Deploy to Streamlit Cloud
        run: |
          echo "Deployment handled via Streamlit Cloud linked to GitHub"
